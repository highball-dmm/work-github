
<h2 class="head-line title_h2 text-center">注文情報確認</h2>
<div calss="row">
  <div class="col-xs-9">
    <% if current_customer.cart_items.present? %>
      <table class="table table-hover table-inverse table-bordered">
        <thead>
          <tr class="info">
            <th>商品名</th>
            <th>単価(税込)</th>
            <th>数量</th>
            <th>小計</th>
          </tr>
        </thead>
        <tbody>
          <% @sum = 0 %>
          <% @cart_items.each do |cart| %>
          <tr>
            <td class="text-left">
              <%= attachment_image_tag( cart.product, :products_image, size: "60x50", fallback: "no_image.jpg") %>
              <%= cart.product.name %>
            </td>
            <td class="text-right">
              ¥<%= (cart.product.non_taxed_price * 1.1).floor %>
            </td>
            <td class="text-right">
              <%= cart.item_quantity %>
            </td>
            <td class="text-right">
              ¥<%= (cart.product.non_taxed_price * 1.1 * cart.item_quantity).floor %>
              <% @sum += (cart.product.non_taxed_price * 1.1 * cart.item_quantity).floor %>
            </td>
          </tr>
          <% end %>
        </tbody>
      </table>
    </div>

    <div class="col-xs-3 billing">
      <table class="table table-hover table-condensed table-bordered">
        <tr>
          <th class="info">送料</th>
          <td class="text-right col-xs-5"><%= @order.shipping %></td>
        </tr>
        <tr>
          <th class="info">商品合計</th>
          <td class="text-right">
            ¥<%= total_price(@cart_items).to_s(:delimited) %>
          </td>
        </tr>
        <tr>
          <th class="info">請求金額</th>
          <td class="text-right">¥<%= billing(@order).to_s(:delimited) %></td>
        </tr>
      </table>
    </div>

    <div class="col-xs-9 billing">
      <table class="table table-hover table-condensed table-bordered">
        <tr>
          <th  class="info">支払方法</th>
          <td class="text-left">
            <% if @order.payment_method = 0 %>
              <p>クレジットカード</p>
            <% else %>
              <p>銀行振り込み</p>
            <% end %>
          </td>
        </tr>
        <tr>
          <th  class="info">お届け先</th>
          <td class="text-left"><%= "#{@order.shipping_postal_code} #{@order.address} #{@order.name}" %></td>
        </tr>
      </table>

      <div class ="col-xs-11">
        <%= form_with model: @order, url: customer_orders_path, method: :post, local: true do |f| %>
          <%= f.hidden_field :shipping_postal_code %>
          <%= f.hidden_field :address %>
          <%= f.hidden_field :name %>
          <%= f.hidden_field :payment_method %>
          <%= f.hidden_field :billing, value: @order.billing %>
          <%= f.hidden_field :shipping, value: @order.shipping.to_i %>
          <%= link_to "注文情報入力へ戻る", new_customer_order_path, data: {"turbolinks" => false},class: "glyphicon glyphicon-folder-open btn btn-info pull-right"  %>
          <%= f.submit "購入を確定する",class: "btn btn-success col-xs-5" %>
        <% end %>
      </div>
    <% else %>
    <p>お客様のショッピングカートに商品はありませんので注文確定ができません。</p>
    <p><%= link_to "商品一覧", products_path %>から商品をお探し下さい。</p>
    <% end %>
  </div>
</div>
